Prime Polo recovery build: 6.1-intake-storage-fix

Fixes:
- Supports current Supabase sb_secret_ server keys correctly
- Does not send opaque sb_secret_ keys as Bearer JWTs
- Still supports legacy service_role JWT keys
- Returns clear errors for missing table, rejected key or missing configuration
- Includes Influencer, Company and Agency intake profiles
- Saves full occupation details and selected services

Required setup:
1. Run supabase/marketing_intakes.sql in Supabase SQL Editor
2. Add SUPABASE_URL and SUPABASE_SECRET_KEY in Vercel
3. Disable Vercel Deployment Protection for preview testing, or use the production domain
4. Redeploy
